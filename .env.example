# bin/dev points the client at its slice's own dev-api, and `vite build` reads
# .env.production. Set this only to point a dev client somewhere else.
# VITE_API_URL=https://parallax.smallworkshop.dev/api/
